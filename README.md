# ansibleaws
gerrit trigger > jenkins > git clone > maven build, test to nexus repository > 
with ansible playbook from nexus get maven artifact create ec2 aws, tomcat 2 servers, 1 apache proxy and 1 mysql server 
